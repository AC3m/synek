# Synek — Claude Code Instructions

Follow `AGENTS.md` first. This file is Claude-specific context only, not a canonical source of project rules.

Canonical guidance lives in:

- `docs/constitution.md` — project principles, locked stack, quality gates
- `docs/reference/` — conventions and anti-patterns
- `docs/how-to/` — implementation recipes
- `docs/architecture/` — architecture decisions and integration details
